source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins

# Ruby 3.4+ split these out of the default gems; github-pages still expects
# them to just be there, so they need to be listed explicitly.
gem "csv"
gem "webrick"
gem "bigdecimal"
gem "logger"
gem "base64"
gem "mutex_m"
gem "ostruct"
gem "benchmark"
gem "drb"
